// vim: ft=javascript
Ext.onReady(function(){
	orgNew().show();

	Ext.ns('App');

	Ext.create('Ext.container.Viewport', {
		layout: 'fit',
		items: Ext.create('Ext.panel.Panel', {
			layout: { type: 'hbox', align: 'stretch' },

			// Main toolbar
			tbar: new App.MainTB(),

			items: [
				// Left panel with Orgs(2 of them: [my, all] as accordion)
				Ext.create('Ext.panel.Panel', {
					flex: 1, layout: 'accordion',
					items: [ new App.OrgMy(), new App.OrgMy() ]
				}),
				// Center panel with Tasks(2 of them: [inwork, done] as accordion)
				Ext.create('Ext.panel.Panel', {
					flex: 2, layout: 'accordion',
					items: [ { html: 'asdasd' }, { html: 'asdasdasd' }
						/*
						// My Tasks
						Ext.create('Ext.grid.GridPanel',{
							title: 'Задачи в работе',
							store: Ext.create('Ext.data.Store', {
								model: 'Sample',
							}),
							headers: [{ name: 'Name' }]
						}),
						// My done Tasks
						Ext.create('Ext.grid.GridPanel',{
							title: 'Выполненные задачи',
							store: Ext.create('Ext.data.Store', {
								model: 'Sample',
							}),
							headers: [{ name: 'Name' }]
						}),
						*/
					]
				})
			]
		})
	});
});
